version: "3.3"  # optional since v1.27.0

networks:
    web:
        external: true
    internal:
        external: false

services:
    divvy:
        build: .
        labels:
        - traefik.http.routers.blog.rule=Host(`api.plellworks.com`)
        - traefik.http.routers.blog.tls=true
        - traefik.http.routers.blog.tls.certresolver=lets-encrypt
        - traefik.port=80
        networks:
        - internal
        - web
